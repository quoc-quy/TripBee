{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "sh -c 'java -jar target/*.jar'",
    "restartPolicyType": "ON_FAILURE"
  }
}
